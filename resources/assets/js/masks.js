$(document).ready(() => {

    // Phone mask
    $('.phone').mask('(99)99999-9999');

    // CEP Mask
    $('.cep').mask('99999-999');

    // Year Mask
    $('.year').mask('9999');

    // Date Mask
    $('.date').mask('99/99/999');

    // RG Mask
    $('.rg').mask('99.999.999-A');

});